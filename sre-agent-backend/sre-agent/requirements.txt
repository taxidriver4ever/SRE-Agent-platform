# HTTP 服务框架与 ASGI 服务器。
fastapi>=0.115,<1.0
# 访问 sre-gateway 的异步 HTTP 客户端。
httpx>=0.27,<1.0
# Agent 决策、请求和响应的数据验证。
pydantic>=2.10,<3.0
# standard extra 包含本地运行常用的事件循环和热重载依赖。
uvicorn[standard]>=0.30,<1.0
# 从未提交到 Git 的 .env 加载本地登录凭据与运行配置。
python-dotenv>=1.0,<2.0
# 读取 service-catalog.yaml 与 Scenario/Skill 元数据，避免把服务映射硬编码在工作流中。
PyYAML>=6.0,<7.0
# MySQL MCP 只用只读账号执行 SELECT/EXPLAIN；PyMySQL 无需本机安装原生驱动。
PyMySQL>=1.1,<2.0
# 使用 FastMCP 提供标准 MCP Server、工具 Schema、参数验证、发现和调用能力。
# 精确锁定稳定版，避免 MCP 生态快速演进时次版本破坏现有 Agent 行为。
fastmcp==3.4.5
