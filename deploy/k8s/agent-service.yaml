apiVersion: v1
kind: Service
metadata:
  name: sre-agent
  namespace: sre
  labels:
    app.kubernetes.io/name: sre-agent
    app.kubernetes.io/part-of: sre-agent-platform
spec:
  selector:
    app.kubernetes.io/name: sre-agent
  ports:
    - name: http
      port: 8001
      targetPort: http
