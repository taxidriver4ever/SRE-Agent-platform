apiVersion: v1
kind: Service
metadata:
  name: sre-agent-frontend
  namespace: sre
  labels:
    app.kubernetes.io/name: sre-agent-frontend
    app.kubernetes.io/part-of: sre-agent-platform
spec:
  selector:
    app.kubernetes.io/name: sre-agent-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
