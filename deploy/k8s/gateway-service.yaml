apiVersion: v1
kind: Service
metadata:
  name: sre-gateway
  namespace: sre
  labels:
    app.kubernetes.io/name: sre-gateway
    app.kubernetes.io/part-of: sre-agent-platform
spec:
  selector:
    app.kubernetes.io/name: sre-gateway
  ports:
    - name: http
      port: 8000
      targetPort: http
